node_modules/
dist/
.env
.env.local
*.log
.DS_Store

# Real private values, used only by scripts/check_privacy.py to scan the build.
# Committing this would defeat its entire purpose.
.private-patterns
probe-out/

# Raw collector payloads. Uploaded as Actions artifacts, NEVER committed - git never
# forgets, so a raw snapshot in history is permanent even after deletion.
raw-out/
